# 参考
Python 数据分析包：pandas 基础 http://www.open-open.com/lib/view/open1402477162868.html
soton 数据分析 链接: http://pan.baidu.com/s/1i4394wt 密码: jsrv

# 问题
无法直接读取csv文件
向dataframe插入数据
pandas中有专门的时间序列方法tseries

#
一般来说，对比Excel，csv的数据都是干净的

时间函数
http://blog.csdn.net/qq_22238533/article/details/70050748
https://docs.scipy.org/doc/numpy/reference/arrays.datetime.html
转换成时间格式     
pd.to_datetime(s, format='%Y%m%d')
获取天数
s.dt.days
获取月份
s.values.astype('datetime64[M]') 
     
d.query('director_name == "Gore Verbinski"') # 只对列条件进行筛选，注意单引号双引号的问题
s.unique
d['month'].astype('str').sort_values().unique() 修改数据类型，series后面直接跟修改函数。unique跟在最后
np.NaN None 是一样的

d['actor_2_name'].isin(['Orlando Bloom']) # 判断是否在列中
d.isnull()
pd.notnull(s) s.notnull() 是一样的，都返回布尔值series

s.shift(1) 偏移函数，整个函数向下移动,和excel上的offset差不多
基本统计量与分布结合起来看


