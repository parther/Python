#!/usr/bin/python3
#是什么 为什么 怎么办
 
#实例：
     拉格朗日插值
     数据归一化
     连续属性离散化
     属性构造
     主成因分析
 
#问题：
     牛顿插值 拉格朗日插值法是什么
     sep=' '
     拉格朗日插值案例中，if (s.isnull())[i]:  if s[i]==None为何不行
     拉格朗日插值案例中，使用if格式，第一行打印出空值
     平稳序列 时间序列 是什么
     ID3算法、Apriori算法 是什么
     pd.read_excel 如何设置索引，列名
     d.max() - d.min()求出的是单列的极值，是否有整组数据的极值
 
#重点：
     进行数据预处理的原因：保证数据完整和准确，提高数据建模的效率
     删除异常值会造成大量的数据浪费，丢失隐藏在其中的信息
     牛顿插值法以后，对插值数据进行整理，对比原始值和插入值
          data[(data[u'销量'] < 400) | (data[u'销量'] > 5000)]返回dataframe，data[u'销量'][(data[u'销量'] < 400) | (data[u'销量'] > 5000)]返回series
          format 的用法： http://www.runoob.com/python/att-string-format.html
               a = format(0.85003330225123219, '.4%')
               a = "{:.2f}".format(0.85003330225123219)
          格拉朗日插值 使用的数据不允许有空值
          格拉朗日插值函数 lagrange(y.index, list(y))(n)
          列表可以相加 a = [1,2,3] + [4,5,6]
          s2 = s.notnull() s2 = s.isnull() 返回布尔值
          copy() 返回拷贝
          2**3 表示多少次方
          np.log10(x) 表示 求对数 放到专门位置
          np.ceil 返回不小于x的最小整数
          df.sort_index(axis=1) ，dataframe排序
               http://www.open-open.com/lib/view/open1402477162868.html
     对于异常值，先寻找异常值出现的原因
     数据集成。将多个数据源（数据表格）统一规范，就是整合的意思
          同一属性命名不一致导致重复，可以使用corr函数识别
     数据变换对数据规范化处理，将数据转换成适当的形式，挖掘和算法的需要
          将不具有正态分布的数据变成具有正态分布的数据，将非平稳序列变为平稳序列
          归一化，将数据进行缩放落到特定区域。不同指标往往具有不同的量纲，消除指标之间量纲和取值范围差异的影响。通常用于基于距离的挖掘算法
               x -min /max - min
               x - x^- / σ
               x / 10^k
          连续数据离散化。将一组数据在取值范围内设定若干点，划分为离散区间。分类挖掘算法需要数据是分类属性的形式 
          属性构造。构造新的属性。挖掘更深次的模式，提高挖掘的精度。
          小波变换 信号分析手段，图像及语言领域
               多分辨率 时域频域 表征信号局部特征 伸缩平移对信号进行多尺度聚焦分析 提供非平稳信号时频分析手段
               能够刻画某问题特征量往往隐藏在信号的某几个分量之中，小波变换将非平稳信号分解为数据序列，即小波系数
               模：向量长度，取余数
               ψ是波函数  普西
               同傅里叶分析 https://www.zhihu.com/question/22864189
               scipy.io pywt 处理小波变换
     数据规约 数据量大的分析和挖掘需要花费大量的时间，数据规约产生量小但保持数据完整性的新数据集，提高效率
          删除不相关属性
          主成因分析 连续属性的数据降维方法 使用少数的新变量能解释原始变量 代替原始数据建模
          数值规约 减小行数据量
               有参数方法，使用模型评估数据，如回归和对数线性模式
               无参数方法 直方图、聚类、抽样
 
 
 
