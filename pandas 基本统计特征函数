# 
基本统计量函数：
d.describe()

累计求和：
d.cumsum()
s.cumsum()
滚动求和：
s.rolling(3).sum() # 3 表示相邻三行 
d.rolling(3).sum()

将数据分成不同的范围组：
pd.cut(s,3,labels=['a','b','c'])

计算series中每个值value的个数：
s.value_counts()

将函数作用到数据集的函数：
1 applymap 
针对DataFrame里的所有单个数据起作用 
d.applymap(lambda x : 1 if x>1 else None if x==0 else 0)

2 apply,agg 
d.apply('std')
d.apply(pd.value_counts)
d.apply(lambda x: x.value_counts)
agg，apply 都传入函数作用于dataframe，但agg只能作用在列
apply中，第二个参数是所引入函数的第二个参数

判断是否在列中:
d['actor_2_name'].isin(['Orlando Bloom']) 
判断空值返回布尔值：
s.isnull() 

偏移数据表：
s.shift(1) 偏移函数，整个函数向下移动,和excel上的offset差不多


