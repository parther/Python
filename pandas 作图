# 问题：
饼图

===

使用subplot画多个子图，比较不同维度:
d.plot(kind='bar', subplots=True)

# 
散点图：
d.plot.scatter(x='a',y='b')
直方图：
d.hist()
d.plot.hist()
d.plot(kind ='hist')
面积图：
d.plot.area()
分组箱线图:
d.boxplot(column = 'avg_salary', by = 'city',figsize = (9,7)) 


